name: Build Lucky for ImmortalWrt x86_64
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - name: 1. 安装基础依赖
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
          gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget curl tar zstd

      - name: 2. 下载 ImmortalWrt 25.12 SDK (x86_64)
        run: |
          wget -O openwrt-sdk.tar.zst https://downloads.immortalwrt.org/releases/25.12.0-rc2/targets/x86/64/immortalwrt-sdk-25.12.0-rc2-x86-64_gcc-14.3.0_musl.Linux-x86_64.tar.zst
          tar --zstd -xf openwrt-sdk.tar.zst
          mv immortalwrt-sdk-* sdk

      - name: 3. 添加 Lucky 源码
        run: |
          cd sdk
          git clone https://github.com/gdy666/luci-app-lucky.git package/lucky

      - name: 4. 配置编译选项
        run: |
          cd sdk
          cat > .config << EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_PACKAGE_lucky=y
          CONFIG_PACKAGE_luci-app-lucky=y
          CONFIG_PACKAGE_luci-i18n-lucky-zh-cn=y
          EOF
          make defconfig

      - name: 5. 开始编译 Lucky (详细日志)
        run: |
          cd sdk
          # 编译并输出详细日志，方便排查
          make package/lucky/compile -j$(nproc) V=s

      - name: 6. 查找并复制 Lucky 相关的 IPK 文件
        run: |
          echo "===== 开始查找所有生成的 IPK 文件 ====="
          # 查找所有 ipk 文件，并过滤出 lucky 相关的
          find sdk -name "*.ipk" | grep -E "(lucky|lucky-app|lucky-i18n)" || echo "⚠️ 未找到 Lucky 相关的 IPK 文件"
          
          echo "===== 开始复制 Lucky 相关的 IPK 文件 ====="
          mkdir -p $GITHUB_WORKSPACE/release_assets
          
          # 复制所有匹配 lucky 的 ipk 文件
          find sdk -name "*.ipk" | grep -E "(lucky|lucky-app|lucky-i18n)" | while read -r file; do
            echo "复制文件: $file"
            cp "$file" $GITHUB_WORKSPACE/release_assets/
          done
          
          echo "===== release_assets 目录内容 ====="
          ls -la $GITHUB_WORKSPACE/release_assets/

      - name: 7. 上传 IPK 到 Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "lucky-v2.19.5-x86_64-25.12"
          files: release_assets/*.ipk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
